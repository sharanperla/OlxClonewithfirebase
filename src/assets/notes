<div>
  {people.filter(person => person.age < 60).map(filteredPerson => (
    <li>
      {filteredPerson.name}
    </li>
  ))}

  .map((element)=>{
    element.filter((obj)=>{obj.productname.match(searchInput)})
  })
</div>